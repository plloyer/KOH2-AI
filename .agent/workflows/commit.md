---
description: Automatically stage and commit changes
---

// turbo-all
1. Stage all changes:
   `git add .`
2. Commit with a descriptive message:
   `git commit -m "[Brief description of changes]"`
